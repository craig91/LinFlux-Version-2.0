import React from 'react';
import ReactBootstrap from 'react-bootstrap';


export default React.createClass({
    render: function (){
        return(
            <div className="pull-right"> 
               <button bsStyle='primary' className="pull-right"> First button </button>
            </div>

        )
    }
})

